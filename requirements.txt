fastapi
uvicorn[standard]
sqlalchemy
psycopg2-binary
pyjwt
passlib[bcrypt]
# Pinned: passlib's bcrypt backend probes bcrypt.__about__, which was
# removed in bcrypt>=4.0.0 — this pin is what keeps password hashing from
# breaking on a fresh install. Do not remove without switching auth libs.
bcrypt<4.0.0
python-multipart
email-validator
requests
openpyxl
python-pptx
reportlab
pandas

# Background jobs — real queue, not FastAPI BackgroundTasks (see api/queue.py, worker.py)
redis
rq

# Optional S3-compatible object storage backend (api/storage.py, only
# imported when STORAGE_BACKEND=s3)
boto3
